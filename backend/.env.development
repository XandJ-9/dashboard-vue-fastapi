# 开发环境配置

# 数据库配置
DB_TYPE=sqlite
# 如果需要使用PostgreSQL，取消下面的注释并配置正确的连接信息
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/admin_template_dev

# 安全配置
SECRET_KEY=dev-secret-key-not-secure
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

# 应用配置
API_PREFIX=/api
BACKEND_CORS_ORIGINS=["http://localhost:3000"]
PROJECT_NAME=Admin Template API (Development)
DEBUG=True